// A Car object with make (string), model (string), and year (number).


let Car:{
    make: string,
    model: string,
    year: number
};


Car = {
    make: "Make of the car",
    model: "Model of the car",
    year: 2025
};

console.log(Car);