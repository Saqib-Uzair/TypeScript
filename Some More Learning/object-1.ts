// A User object with id (number), name (string), and isAdmin (boolean).

let User:{
    id:number,
    name:string,
    isAdmin:boolean
};

User={
    id:25,
    name:"John",
    isAdmin:false
}

console.log(User)