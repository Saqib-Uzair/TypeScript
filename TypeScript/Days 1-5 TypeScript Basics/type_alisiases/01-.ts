// Create a type alias Person that has the following properties:

//     name: a string
//     age: a number
//     isEmployed: a boolean

// Declare a variable of type Person and assign it some values.

type PersonAliases= string | number | boolean;

let person_name: PersonAliases= "saqib uzair";
let person_age: PersonAliases = 25;
let isEmployed: PersonAliases = true;

console.log(person_name)
console.log(person_age)
console.log(isEmployed)